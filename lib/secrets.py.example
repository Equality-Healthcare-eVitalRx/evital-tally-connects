# Copy this file to secrets.py and fill in your values.
# secrets.py is gitignored and will NOT be pushed to GitHub.
#
# The GitHub repo is public - git operations use an SSH deploy key
# on the build machine and the Releases API needs no token.

ENCRYPTION_KEY = ""
